# 公开仓库独立敏感扫描报告

本文件由导出脚本 `build_public_release.py` **自动生成**，每次导出覆盖刷新。
生成方式：`git archive HEAD`（只取内部仓库已跟踪文件）→ 代号替换 → 覆盖公开专有文件
（LICENSE/SECURITY/合成示例）→ 全量扫描 → 在导出结果里跑公开测试。

## 1. 扫描范围

| 项 | 值 |
|---|---|
| 内部仓库跟踪文件 | 72 |
| 导出后文件总数（含公开专有文件） | 79 |
| 实际扫描文件数（文本 + xlsx/json/md/txt/yaml/toml…） | 79 |
| 敏感命中 | **0** |

扫描对象覆盖全部文本文件（含无扩展名的 `LICENSE`）与 `xlsx/json/md/txt/yaml/toml`；
模式（黑名单词表）本身保存在仓库之外的内部脚本里，**不随公开仓库发布**——
本文件只公布类别与命中数。

## 2. 逐类命中

| 类别 | 命中 |
|---|---|
| 真实项目名(中文) | 0 |
| 真实项目名(拼音) | 0 |
| 招标编号 | 0 |
| 人员姓名 | 0 |
| 手机号 | 0 |
| 身份证号 | 0 |
| 证书编号 | 0 |
| 统一社会信用代码 | 0 |
| 合同编号 | 0 |
| 本机绝对路径 | 0 |
| token/密钥 | 0 |

## 3. 命中明细

无命中。

## 4. 公开环境测试结果

```
Ran 93 tests in 0.857s
OK (skipped=25)
```

| skip 原因 | 次数 |
|---|---|
| 缺少本地项目配置 config/sample_a.json（不入库） | 9 |
| 缺少本地项目配置或投标文件文本 fixture（不入库） | 6 |
| 缺少项目配置 config/sample_a.json（真实项目配置不入库） | 5 |
| run-005-final 尚未生成 | 2 |
| 对照项目产物尚未生成（本地） | 1 |
| 阶段报告为本地私有文档，未纳入仓库 | 1 |
| 本机没有可检查的运行产物（真实项目产物不入库） | 1 |

说明：skip 是**设计行为**——需要真实语料（招标文件、投标文件、真实项目配置）的用例
在公开仓库里自动跳过，并在原因里写明缺什么。要求是 **FAIL = 0**。

## 5. 结论

| 门槛 | 结果 |
|---|---|
| 敏感命中（未解释） | **0** → 门槛「必须为 0」：通过 |
| 公开测试 FAIL / ERROR | **0** → 门槛「必须为 0」：通过 |
| 私有目录（fixtures/ projects/ reports/ review-package/） | 未出现 |
| 真实项目配置 config/*.json、私有 Gold oracle | 未出现 |

以上数据即为本次导出快照的可审计证据，可作为 push 前的放行依据。
