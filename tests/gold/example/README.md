# 示例 Gold oracle

本目录是**合成**的 Gold 期望值样例，用于说明格式与度量口径。

真实项目的 oracle（含招标条款原文 token、材料 ID、判定期望）属于 PRIVATE_TEST_DATA，
不入库；本地位置见 `projects/_local_oracles/<profile>/`。

度量器实现见 `tests/gold/oracle.py`，评分入口见 `scripts/score_oracle.py`。
