# 数据边界与安全说明（SECURITY）

本仓库是 **JTY-BidCompiler 的公开镜像**：只包含**代码与契约**，不含任何真实生产数据。

## 1. 数据边界

| 类别 | 是否在本仓库 | 说明 |
|---|---|---|
| 源码 `src/`、规则 `rules/`、Schema `schemas/`、脚本 `scripts/` | ✅ 在 | 与具体项目、投标人、招标人无关 |
| 配置模板 `config/example.json` | ✅ 在 | 全是占位符 |
| **合成示例** `config/synthetic.json`、`tests/synthetic/**` | ✅ 在 | 项目、公司、人员、证件号均为虚构，见文件内 `_note` |
| 真实招标文件 / 投标文件 / 核验表 / 扫描件 | ❌ 不在 | 本地目录保留，`.gitignore` 排除 |
| 真实投标人素材库（人员、证件号、合同金额、证据原文） | ❌ 不在 | 同上 |
| 真实项目运行产物（结构化中间件、Excel、评分报告、交付包） | ❌ 不在 | 同上 |
| 真实项目的 Gold oracle（含招标条款原文 token） | ❌ 不在 | 公开仓库只留 `tests/gold/example/` 的合成样例 |
| 凭据（token / key / 密码 / 私钥） | ❌ 不在 | `.gitignore` 兜底排除 |

**声明：本仓库中的项目、公司、人员、证书、合同和投标数据均为合成示例或脱敏测试数据；真实生产数据不包含在公开仓库中。**

## 2. 为什么按"数据不入库的人"设计

编译器的输入（招标文件、投标文件、公司素材）几乎全是**商业秘密**：它们一旦进入 Git 历史，即使之后删除文件，blob 仍留在历史里。因此本项目的纪律是：

1. **输入目录从不入库**（`fixtures/`），只留 `fixtures/README.md` 说明目录约定。
2. **输出目录从不入库**（`projects/`、`reports/`、`review-package/`）。
3. **真实项目配置从不入库**（`config/*.json`），只留 `config/example.json` 与 `config/synthetic.json`。
4. **代码只读不写外部目录**：产物一律落在本仓库内的输出目录；源项目目录只读。

## 3. 提交前自检（建议本地执行）

```bash
# 1) 确认没有把私有目录加进暂存区
git status --short

# 2) 扫描暂存内容里是否出现真实项目/人员/证件等标识
#    （下例中的证件号写法刻意写成正则片段，避免本文件自己命中扫描）
git diff --cached --name-only -z | xargs -0 grep -nIE \
  "CCTC[0-9]{4,}|1[3-9][0-9]{9}|[0-9]{17}[0-9Xx]|D214[0-9]{5}|JZ[A-Za-z]*安许证字" || echo "无命中"
```

命中即**不要提交**；把该文件移出跟踪范围（加入 `.gitignore`），并在本地保留原文件。

## 4. 报告问题

如发现本仓库中存在疑似真实数据（项目名、公司名、人员姓名、证件号、合同编号、金额、本机绝对路径等），请**不要**在公开 issue 里粘贴原文，直接联系维护者，我们会先在源码仓库修正、再重新导出公开快照。
