"""Word 自动装配层（MVP-6～9）——**本阶段刻意留空**。

为什么留空而不是先写个能跑的版本：Word 装配的质量取决于上游判定是否可靠。
MVP-1～5 若不可靠，章节生成得再漂亮也只是把错误包装得更体面。因此第一阶段
只把接口位置与约束条件记下来，不写任何"看起来能跑"的假实现。

接入时必须遵守的长文档经验（详见 docs/JTY-BidCompiler架构设计.md §6）：

* media 原始字节直通——101MB/339 页文档内嵌 193 张图，任何重编码都会让导出崩掉；
* 扫描件 DrawingML 旋转属性原样携带，不做几何重算；
* 目录只用 Word 更新（TOC 域），**禁止用 LibreOffice 另存**（跨引擎重排会丢内容）；
* 分节与页脚：封面+目录无页码，正文自"一、投标函"起"第 X 页"重新起算；
* 表格跨页重复标题行，标题不得与证据图分离；
* 全链路 SHA256，便于冻结与复现；
* 100MB 级文档 COM 导出 PDF 极慢（实测 >15 分钟无产出），必须独立进程 + 超时 + 降级。

同时必须守住的边界：报价、税率、税率、人员名单、签章一律**不自动填写**，
只生成空白位并登记为待办——这属于公司真实事项。
"""

#: MVP-6～9 规划的模块名（占位，未实现）
PLANNED_MODULES = (
    "commercial",     # MVP-6 商务章节（投标函/报价表骨架/保证金页/偏差表/业绩统计表）
    "technical",      # MVP-7 技术方案模块（服务方案/HSE/质量/机具/售后）
    "docx_assembler", # MVP-8 Word 装配（media 直通、分节、页脚、TOC、SHA256）
    "pdf_qa",         # MVP-9 PDF/版式 QA（页数、孤悬标题、长图跨页、页脚连续）
)
